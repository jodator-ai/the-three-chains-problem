# Auto-generated by generate-prividium-compose.sh — do not edit manually.
# Prividium instance for chain ID: 6565
# Ports: admin=3000 user=3001 api=8000 keycloak=5080 zksyncos=5050
#
# Includes docker-compose-deps-6565.yaml (zksyncos, keycloak, block-explorer)
# which in turn includes docker-compose-l1.yaml (Anvil L1, shared postgres).
# A single: docker compose -f docker-compose-6565.yaml up
# brings up the full stack.
name: zksync-prividiums

include:
  - docker-compose-deps-6565.yaml

services:

  # ── prividium-api ────────────────────────────────────────────────────────────
  prividium-api-6565:
    ## REMOVE PUBLIC:START
    build:
      context: .
      dockerfile: ./apps/permissions-api/Dockerfile
    ## REMOVE PUBLIC:END
    ## PUBLIC:image: quay.io/matterlabs_enterprise/prividium-permissions-api:v1.153.1
    ## PUBLIC:platform: linux/amd64
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      zksyncos-6565:
        condition: service_healthy
    ports:
      - '8000:8000'
      - '9091:9091'
    environment:
      - PORT=8000
      - METRICS_PORT=9091
      - SEQUENCER_RPC_URL=http://zksyncos-6565:3050
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/prividium_api_6565
      - CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://localhost:3002
      - AUTH_METHODS=oidc,crypto_native
      - NODE_ENV=production
      - SIWE_CHAIN_ID=6565
      - SIWE_VALID_DOMAINS=localhost:3000,localhost:3001
      - SIWE_HMAC_SECRET=aaaaaaaa00000000aaaaaaaa00000000aaaaaaaa00000000aaaaaaaa00000000
      - OIDC_JWKS_URI=http://keycloak-6565:8080/realms/prividium/protocol/openid-connect/certs
      - OIDC_JWT_AUD=prividium-client
      - OIDC_JWT_ISSUER=http://localhost:5080/realms/prividium
      - OIDC_ADMIN_SUBS=00000000-0000-0000-0000-000000000001
      - CORS_CACHE_DURATION_MS=600000
      - ADMIN_PANEL_REDIRECT_URLS=http://localhost:3000/callback
      - BLOCK_EXPLORER_REDIRECT_URLS=http://localhost:3010/auth/callback
      - WALLETS_API_ENABLED=true
      - WEBAUTHN_RP_NAME=ZKsync Prividium
      - WEBAUTHN_RP_ID=localhost
      - WEBAUTHN_ORIGIN=http://localhost:3001
      - WEBAUTHN_REQUIRE_USER_VERIFICATION=false
      - EXTRA_PUBLIC_CODE_ADDRESSES=0x36615cf349d7f6344891b1e7ca7c72883f5dc049

  # ── admin panel ──────────────────────────────────────────────────────────────
  admin-panel-6565:
    ## REMOVE PUBLIC:START
    build:
      context: .
      dockerfile: ./apps/adminv2/Dockerfile
    ## REMOVE PUBLIC:END
    ## PUBLIC:image: quay.io/matterlabs_enterprise/prividium-adminv2:v1.153.1
    ## PUBLIC:platform: linux/amd64
    restart: unless-stopped
    environment:
      - VITE_PRIVIDIUM_API_URL=http://localhost:8000
      - VITE_USER_PANEL_URL=http://localhost:3001
      - VITE_EXPLORER_URL=http://localhost:3010
      - PORT=3000
    depends_on:
      - prividium-api-6565
    ports:
      - '3000:3000'

  # ── user panel ───────────────────────────────────────────────────────────────
  user-panel-6565:
    ## REMOVE PUBLIC:START
    build:
      context: .
      dockerfile: ./apps/user-panel/Dockerfile
    ## REMOVE PUBLIC:END
    ## PUBLIC:image: quay.io/matterlabs_enterprise/prividium-user-panel:v1.153.1
    ## PUBLIC:platform: linux/amd64
    restart: unless-stopped
    environment:
      - VITE_OIDC_AUTHORITY=http://localhost:5080/realms/prividium
      - VITE_OIDC_CLIENT_ID=prividium-client
      - VITE_OIDC_REDIRECT_URI=http://localhost:3001/callback
      - VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:3001/login
      - VITE_OIDC_BUTTON_TEXT=Sign in with Keycloak
      - VITE_ADMIN_PANEL_REDIRECT_URI=http://localhost:3000/callback
      - VITE_AUTH_METHODS=oidc,crypto_native
      - VITE_CHAIN_ID=6565
      - VITE_CHAIN_NAME=Prividium Local
      - VITE_WALLETS_ENABLED=true
      - VITE_BRIDGING_ENABLED=true
      - VITE_L1_RPC_URL=http://localhost:5010
      - VITE_L1_CHAIN_ID=31337
      - VITE_L1_CHAIN_NAME=Anvil Localhost
      - VITE_PRIVIDIUM_API_URL=http://localhost:8000
      - VITE_EXPLORER_URL=http://localhost:3010
      - VITE_ADMIN_PANEL_URL=http://localhost:3000
      - VITE_REOWN_PROJECT_ID=
      - PORT=3000
    depends_on:
      - prividium-api-6565
    ports:
      - '3001:3000'
