# Auto-generated by generate-prividium-compose.sh — do not edit manually.
# Prividium instance for chain ID: 6567
# Ports: admin=3400 user=3401 api=8400 keycloak=5480 zksyncos=5450
#
# Includes docker-compose.6567.deps.yml (zksyncos, keycloak, block-explorer)
# which in turn includes docker-compose.l1.yml (Anvil L1, shared postgres).
# A single: docker compose -f docker-compose.6567.yml up
# brings up the full stack.
name: zksync-prividiums

include:
  - docker-compose.6567.deps.yml

services:

  # ── prividium-api ────────────────────────────────────────────────────────────
  prividium-api-6567:
    image: quay.io/matterlabs_enterprise/prividium-permissions-api:v1.169.1
    platform: linux/amd64
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      zksyncos-6567:
        condition: service_healthy
      bundler-6567:
        condition: service_started
    ports:
      - '8400:8000'
      - '9491:9091'
    environment:
      - PORT=8000
      - METRICS_PORT=9091
      - SEQUENCER_RPC_URL=http://zksyncos-6567:3052
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/prividium_api_6567
      - CORS_ORIGIN=http://localhost:3400,http://localhost:3401,http://localhost:3410,http://localhost:3402
      - AUTH_METHODS=oidc,crypto_native
      - NODE_ENV=production
      - SIWE_CHAIN_ID=6567
      - SIWE_VALID_DOMAINS=localhost:3400,localhost:3401,localhost:3410,localhost:3402
      - SIWE_HMAC_SECRET=aaaaaaaa00000000aaaaaaaa00000000aaaaaaaa00000000aaaaaaaa00000000
      - OIDC_JWKS_URI=http://keycloak-6567:8080/realms/prividium/protocol/openid-connect/certs
      - OIDC_JWT_AUD=prividium-client
      - OIDC_JWT_ISSUER=http://localhost:5480/realms/prividium
      - OIDC_ADMIN_SUBS=00000000-0000-0000-0000-000000000001
      - CORS_CACHE_DURATION_MS=600000
      - ADMIN_PANEL_REDIRECT_URLS=http://localhost:3400/callback
      - BLOCK_EXPLORER_REDIRECT_URLS=http://localhost:3410/auth/callback
      - WALLETS_API_ENABLED=true
      - WEBAUTHN_RP_NAME=ZKsync Prividium
      - WEBAUTHN_RP_ID=localhost
      - WEBAUTHN_ORIGIN=http://localhost:3401
      - WEBAUTHN_REQUIRE_USER_VERIFICATION=false
      - EXTRA_PUBLIC_CODE_ADDRESSES=0x36615cf349d7f6344891b1e7ca7c72883f5dc049
      - BUNDLER_ENABLED=true
      - BUNDLER_RPC_URL=http://bundler-6567:4337
      - RATE_LIMIT_ENABLED=true
      - RATE_LIMIT_AUTH_MAX=100
      - RATE_LIMIT_PUBLIC_MAX=300
      - RATE_LIMIT_USER_MAX=300
      - RATE_LIMIT_RPC_MAX=1000
      - RATE_LIMIT_WINDOW_MS=60000

  # ── admin panel ──────────────────────────────────────────────────────────────
  admin-panel-6567:
    image: quay.io/matterlabs_enterprise/prividium-adminv2:v1.169.1
    platform: linux/amd64
    restart: unless-stopped
    environment:
      - VITE_PRIVIDIUM_API_URL=http://localhost:8400
      - VITE_USER_PANEL_URL=http://localhost:3401
      - VITE_EXPLORER_URL=http://localhost:3410
      - PORT=3000
    depends_on:
      - prividium-api-6567
    ports:
      - '3400:3000'

  # ── user panel ───────────────────────────────────────────────────────────────
  user-panel-6567:
    image: quay.io/matterlabs_enterprise/prividium-user-panel:v1.169.1
    platform: linux/amd64
    restart: unless-stopped
    environment:
      - VITE_OIDC_AUTHORITY=http://localhost:5480/realms/prividium
      - VITE_OIDC_CLIENT_ID=prividium-client
      - VITE_OIDC_REDIRECT_URI=http://localhost:3401/callback
      - VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:3401/login
      - VITE_OIDC_BUTTON_TEXT=Sign in with Keycloak
      - VITE_ADMIN_PANEL_REDIRECT_URI=http://localhost:3400/callback
      - VITE_AUTH_METHODS=oidc,crypto_native
      - VITE_CHAIN_ID=6567
      - VITE_CHAIN_NAME=Prividium Local
      - VITE_WALLETS_ENABLED=true
      - VITE_BRIDGING_ENABLED=true
      - VITE_L1_RPC_URL=http://localhost:5010
      - VITE_L1_CHAIN_ID=31337
      - VITE_L1_CHAIN_NAME=Anvil Localhost
      - VITE_PRIVIDIUM_API_URL=http://localhost:8400
      - VITE_EXPLORER_URL=http://localhost:3410
      - VITE_ADMIN_PANEL_URL=http://localhost:3400
      - VITE_REOWN_PROJECT_ID=
      - PORT=3000
    depends_on:
      - prividium-api-6567
    ports:
      - '3401:3000'
