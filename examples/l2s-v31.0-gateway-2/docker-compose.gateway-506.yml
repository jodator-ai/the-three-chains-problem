# Auto-generated by generate-compose.sh — do not edit manually.
# Gateway chain (ID: 506) — settles to L1; L2 chains settle here.
# See: https://github.com/matter-labs/zksync-os-server/tree/main/local-chains/v31.0/gateway
name: zksync-l2s

services:
  gateway-506:
    image: ghcr.io/matter-labs/zksync-os-server:latest
    platform: linux/amd64
    ports:
      - '5049:3052'
    environment:
      GENERAL_L1_RPC_URL: 'http://l1:5010'
    user: 'root'
    working_dir: '/app'
    entrypoint: ''
    command: ['/usr/bin/tini', '--', 'zksync-os-server', '--config', '/configs/chain_506.yaml']
    volumes:
      - gateway_506_db:/db
      - ./chain_506.yaml:/configs/chain_506.yaml:ro
      - ./genesis.json:/app/local-chains/v31.0/genesis.json:ro
      - ./gateway-db.tar.gz:/app/local-chains/v31.0/gateway-db.tar.gz:ro
    depends_on:
      l1:
        condition: service_healthy
    healthcheck:
      test: ['CMD', '/usr/bin/bash', '-c', 'exec 3<>/dev/tcp/127.0.0.1/3052']
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

volumes:
  gateway_506_db:
