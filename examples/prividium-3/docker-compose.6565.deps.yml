# Auto-generated by generate-prividium-compose.sh — do not edit manually.
# Backend deps for Prividium instance chain ID: 6565
# (zksyncos sequencer, keycloak, block explorer)
# Included by docker-compose.6565.yml via the include: directive.
name: zksync-prividiums

include:
  - docker-compose.l1.yml

services:

  # ── zksyncos sequencer ───────────────────────────────────────────────────────
  zksyncos-6565:
    image: ghcr.io/matter-labs/zksync-os-server:latest
    platform: linux/amd64
    ports:
      - '5050:3050'
    environment:
      GENERAL_L1_RPC_URL: 'http://l1:5010'
    user: 'root'
    working_dir: '/app'
    entrypoint: ''
    command: ['/usr/bin/tini', '--', 'zksync-os-server', '--config', '/configs/chain_6565.yaml']
    volumes:
      - zksyncos_6565_db:/db
      - ./dev/prividium-1/zksyncos/chain_6565.yaml:/configs/chain_6565.yaml:ro
      - ./dev/l1/genesis.json:/app/local-chains/v30.2/genesis.json:ro
    depends_on:
      l1:
        condition: service_healthy
    healthcheck:
      test: ['CMD', '/usr/bin/bash', '-c', 'exec 3<>/dev/tcp/127.0.0.1/3050']
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  # ── keycloak ────────────────────────────────────────────────────────────────
  keycloak-6565:
    image: quay.io/keycloak/keycloak:26.0
    ports:
      - '5080:8080'
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HTTP_RELATIVE_PATH: /
      KC_HEALTH_ENABLED: 'true'
      KC_HTTP_ENABLED: 'true'
      KC_HOSTNAME_STRICT: 'false'
      KC_HOSTNAME_STRICT_HTTPS: 'false'
      KC_HOSTNAME_URL: 'http://localhost:5080'
      KC_HOSTNAME_ADMIN_URL: 'http://localhost:5080'
    command:
      - start-dev
      - --import-realm
    volumes:
      - ./dev/prividium-1/keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json:ro
    healthcheck:
      test: ['CMD-SHELL', 'exec 3<>/dev/tcp/127.0.0.1/8080']
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 60s

  # ── block explorer ───────────────────────────────────────────────────────────
  block-explorer-api-6565:
    image: ghcr.io/matter-labs/block-explorer-api:latest
    platform: linux/amd64
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      LOG_LEVEL: verbose
      NODE_ENV: development
      DATABASE_HOST: postgres
      DATABASE_NAME: prividium_block_explorer_6565
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres
      DATABASE_ENABLE_SSL: 'false'
      NETWORK_NAME: testnet
      PRIVIDIUM: 'true'
      PRIVIDIUM_APP_URL: http://localhost:3010
      PRIVIDIUM_PERMISSIONS_API_URL: http://host.docker.internal:8000
      PRIVIDIUM_SESSION_SECRET: QkxPQ0tfRVhQTE9SRVJfU0VTU0lPTl9TRUNSRVQ=
      PRIVIDIUM_SESSION_MAX_AGE: 86400000
      PRIVIDIUM_SESSION_SAME_SITE: strict
      LIMITED_PAGINATION_MAX_ITEMS: '10000'
      API_LIMITED_PAGINATION_MAX_ITEMS: '1000'
      DISABLE_BFF_API_SCHEMA_DOCS: 'true'
    ports:
      - '3002:3000'
    depends_on:
      postgres:
        condition: service_healthy

  block-explorer-data-fetcher-6565:
    image: ghcr.io/matter-labs/block-explorer-data-fetcher:latest
    platform: linux/amd64
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      NETWORK_NAME: testnet
      BLOCKCHAIN_RPC_URL: http://zksyncos-6565:3050
      PRIVIDIUM: 'true'
      PRIVIDIUM_PERMISSIONS_API_URL: http://host.docker.internal:8000/api
    ports:
      - '3040:3040'

  block-explorer-worker-6565:
    image: ghcr.io/matter-labs/block-explorer-worker:latest
    platform: linux/amd64
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      DATABASE_HOST: postgres
      DATABASE_NAME: prividium_block_explorer_6565
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres
      DATABASE_ENABLE_SSL: 'false'
      NETWORK_NAME: testnet
      BLOCKCHAIN_RPC_URL: http://zksyncos-6565:3050
      DATA_FETCHER_URL: http://block-explorer-data-fetcher-6565:3040
      PRIVIDIUM: 'true'
      PRIVIDIUM_PERMISSIONS_API_URL: http://host.docker.internal:8000/api
      ENABLE_TOKEN_OFFCHAIN_DATA_SAVER: 'true'
      BLOCKS_PROCESSING_BATCH_SIZE: '10'
      NUMBER_OF_BLOCKS_PER_DB_TRANSACTION: '10'
    depends_on:
      postgres:
        condition: service_healthy
      block-explorer-data-fetcher-6565:
        condition: service_started

  block-explorer-app-6565:
    image: ghcr.io/matter-labs/block-explorer-app:latest
    platform: linux/amd64
    volumes:
      - ./dev/prividium-1/block-explorer/block-explorer-config.js:/usr/share/nginx/html/config.js:ro
    ports:
      - '3010:3010'
    depends_on:
      block-explorer-api-6565:
        condition: service_started

volumes:
  zksyncos_6565_db:
