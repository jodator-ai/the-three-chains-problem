# Auto-generated by generate-prividium-compose.sh — do not edit manually.
# Backend deps for Prividium instance chain ID: 6567
# (zksyncos sequencer, keycloak, block explorer)
# Included by docker-compose.6567.yml via the include: directive.
name: zksync-prividiums

include:
  - docker-compose.l1.yml

services:

  # ── zksyncos sequencer ───────────────────────────────────────────────────────
  zksyncos-6567:
    image: ghcr.io/matter-labs/zksync-os-server:v0.18.1
    platform: linux/amd64
    ports:
      - '5450:3052'
    environment:
      GENERAL_L1_RPC_URL: 'http://l1:5010'
    user: 'root'
    working_dir: '/app'
    entrypoint: ''
    command: ['/usr/bin/tini', '--', 'zksync-os-server', '--config', '/configs/chain_6567.yaml']
    volumes:
      - zksyncos_6567_db:/db
      - ./dev/prividium-3/zksyncos/chain_6567.yaml:/configs/chain_6567.yaml:ro
      - ./dev/l1/genesis.json:/app/local-chains/v30.2/genesis.json:ro
    depends_on:
      l1:
        condition: service_healthy
    healthcheck:
      test: ['CMD', '/usr/bin/bash', '-c', 'exec 3<>/dev/tcp/127.0.0.1/3052']
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  # ── keycloak ────────────────────────────────────────────────────────────────
  keycloak-6567:
    image: quay.io/keycloak/keycloak:26.0
    ports:
      - '5480:8080'
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HTTP_RELATIVE_PATH: /
      KC_HEALTH_ENABLED: 'true'
      KC_HTTP_ENABLED: 'true'
      KC_HOSTNAME_STRICT: 'false'
      KC_HOSTNAME_STRICT_HTTPS: 'false'
      KC_HOSTNAME_URL: 'http://localhost:5480'
      KC_HOSTNAME_ADMIN_URL: 'http://localhost:5480'
      KC_METRICS_ENABLED: 'true'
    command:
      - start-dev
      - --import-realm
    volumes:
      - ./dev/prividium-3/keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json:ro
    healthcheck:
      test: ['CMD-SHELL', 'exec 3<>/dev/tcp/127.0.0.1/8080']
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 60s

  # ── block explorer ───────────────────────────────────────────────────────────
  block-explorer-api-6567:
    image: ghcr.io/matter-labs/block-explorer-api:latest
    platform: linux/amd64
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      LOG_LEVEL: verbose
      NODE_ENV: development
      DATABASE_HOST: postgres
      DATABASE_NAME: prividium_block_explorer_6567
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres
      DATABASE_ENABLE_SSL: 'false'
      NETWORK_NAME: testnet
      PRIVIDIUM: 'true'
      PRIVIDIUM_APP_URL: http://localhost:3410
      PRIVIDIUM_PERMISSIONS_API_URL: http://host.docker.internal:8400
      PRIVIDIUM_SESSION_SECRET: QkxPQ0tfRVhQTE9SRVJfU0VTU0lPTl9TRUNSRVQ=
      PRIVIDIUM_SESSION_MAX_AGE: 86400000
      PRIVIDIUM_SESSION_SAME_SITE: strict
      LIMITED_PAGINATION_MAX_ITEMS: '10000'
      API_LIMITED_PAGINATION_MAX_ITEMS: '1000'
      DISABLE_BFF_API_SCHEMA_DOCS: 'true'
    ports:
      - '3402:3000'
    depends_on:
      postgres:
        condition: service_healthy

  block-explorer-data-fetcher-6567:
    image: ghcr.io/matter-labs/block-explorer-data-fetcher:latest
    platform: linux/amd64
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      NETWORK_NAME: testnet
      BLOCKCHAIN_RPC_URL: http://zksyncos-6567:3052
      PRIVIDIUM: 'true'
      PRIVIDIUM_PERMISSIONS_API_URL: http://host.docker.internal:8400/api
    ports:
      - '3440:3040'

  block-explorer-worker-6567:
    image: ghcr.io/matter-labs/block-explorer-worker:latest
    platform: linux/amd64
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      DATABASE_HOST: postgres
      DATABASE_NAME: prividium_block_explorer_6567
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres
      DATABASE_ENABLE_SSL: 'false'
      NETWORK_NAME: testnet
      BLOCKCHAIN_RPC_URL: http://zksyncos-6567:3052
      DATA_FETCHER_URL: http://block-explorer-data-fetcher-6567:3040
      PRIVIDIUM: 'true'
      PRIVIDIUM_PERMISSIONS_API_URL: http://host.docker.internal:8400/api
      ENABLE_TOKEN_OFFCHAIN_DATA_SAVER: 'true'
      BLOCKS_PROCESSING_BATCH_SIZE: '10'
      NUMBER_OF_BLOCKS_PER_DB_TRANSACTION: '10'
    depends_on:
      postgres:
        condition: service_healthy
      block-explorer-data-fetcher-6567:
        condition: service_started

  block-explorer-app-6567:
    image: ghcr.io/matter-labs/block-explorer-app:latest
    platform: linux/amd64
    volumes:
      - ./dev/prividium-3/block-explorer/block-explorer-config.js:/usr/share/nginx/html/config.js:ro
    ports:
      - '3410:3010'
    depends_on:
      block-explorer-api-6567:
        condition: service_started

  # ── prometheus ───────────────────────────────────────────────────────────────
  prometheus-6567:
    image: prom/prometheus:v2.48.0
    restart: unless-stopped
    ports:
      - '9490:9090'
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    volumes:
      - ./dev/prividium-3/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_6567_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
    healthcheck:
      test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://localhost:9090/-/healthy']
      interval: 10s
      timeout: 5s
      retries: 5

  # ── grafana ───────────────────────────────────────────────────────────────────
  grafana-6567:
    image: grafana/grafana:10.2.0
    restart: unless-stopped
    ports:
      - '3500:3000'
    volumes:
      - grafana_6567_data:/var/lib/grafana
      - ./dev/prividium-3/grafana/datasources:/etc/grafana/provisioning/datasources:ro
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=http://localhost:3500
    depends_on:
      - prometheus-6567
    healthcheck:
      test: ['CMD-SHELL', 'wget --quiet --tries=1 --spider http://localhost:3000/api/health || exit 1']
      interval: 10s
      timeout: 5s
      retries: 5

  # ── webhook ───────────────────────────────────────────────────────────────────
  zksync-webhook-db-6567:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: zksync_webhook_db
    volumes:
      - zksync_webhook_db_6567_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d zksync_webhook_db']
      interval: 5s
      timeout: 5s
      retries: 10

  zksync-webhook-service-6567:
    image: quay.io/matterlabs_enterprise/webhook-service:v0.1.19
    platform: linux/amd64
    init: true
    restart: unless-stopped
    depends_on:
      zksync-webhook-db-6567:
        condition: service_healthy
    environment:
      DATABASE_HOST: zksync-webhook-db-6567
      DATABASE_PORT: '5432'
      DATABASE_NAME: zksync_webhook_db
      DATABASE_USER: postgres
      DATABASE_PASSWORD: password
      DATABASE_ENABLE_SSL: 'false'
      DATABASE_SSL_REJECT_UNAUTHORIZED: 'true'
      ZKSYNC_WEBHOOK_CONFIG: /app/config/config.prividium.local.toml
      RUST_LOG: info
      ENCRYPTION_KEY: a40c350fed393623dfaa54d93d96addcbf8ae845e4f1b6eeaeb444aa3645e800
      ENCRYPTION_KDF_SALT: zksync-webhook-service:keycipher:v2
      PRIVIDIUM_SIGNER_KEY: ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
      PRIVIDIUM_RPC_URL: http://host.docker.internal:5450
      PRIVIDIUM_PERMISSIONS_API: http://host.docker.internal:8400
      PRIVIDIUM_SIWE_DOMAIN: http://host.docker.internal:3400
      ZKSYNC_WEBHOOK_API_CORS_ALLOW_ORIGIN: http://localhost:3400
      ZKSYNC_WEBHOOK_ALLOW_HTTP_LOCALHOST: 'true'
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    ports:
      - '8480:8080'
      - '8481:8081'

  # ── bundler (ERC-4337) ────────────────────────────────────────────────────────
  bridge-funds-6567:
    image: node:22-slim
    working_dir: /app
    volumes:
      - ./dev/bundler/contracts:/app
      - bridge_funds_6567_node_modules:/app/node_modules
    environment:
      L1_RPC_URL: http://l1:5010
      L2_RPC_URL: http://zksyncos-6567:3052
      PRIVATE_KEY: '0x8b3a350cf5c34c9194ca85829a2df0ec3153be0318b5e2d3348e872092edffba'
    command: sh -c "corepack enable && pnpm install && pnpm tsx scripts/bridge-funds.ts"
    depends_on:
      zksyncos-6567:
        condition: service_healthy

  entrypoint-deployer-6567:
    image: ghcr.io/foundry-rs/foundry:v1.5.1
    entrypoint: ''
    user: 'root'
    volumes:
      - ./dev/bundler/contracts/entrypoint:/app
    working_dir: /app
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      RPC_URL: http://zksyncos-6567:3052
      PRIVATE_KEY: '0x8b3a350cf5c34c9194ca85829a2df0ec3153be0318b5e2d3348e872092edffba'
    command: >
      sh -c "forge soldeer install && ./script/deploy.sh"
    depends_on:
      bridge-funds-6567:
        condition: service_completed_successfully

  bundler-6567:
    image: quay.io/matterlabs_enterprise/prividium-bundler:v1.169.1
    platform: linux/amd64
    restart: unless-stopped
    ports:
      - '4737:4337'
    environment:
      BUNDLER_PORT: '4337'
      SEQUENCER_RPC_URL: http://zksyncos-6567:3052
      BUNDLER_EXECUTOR_PRIVATE_KEYS: '0x8b3a350cf5c34c9194ca85829a2df0ec3153be0318b5e2d3348e872092edffba'
      BUNDLER_UTILITY_PRIVATE_KEY: '0x8b3a350cf5c34c9194ca85829a2df0ec3153be0318b5e2d3348e872092edffba'
      BUNDLER_ENTRYPOINTS: '0x4337084D9E255Ff0702461CF8895CE9E3b5Ff108'
      BUNDLER_MIN_BALANCE: '0'
      BUNDLER_NETWORK_NAME: 'local'
      BUNDLER_LOG_LEVEL: 'info'
    depends_on:
      entrypoint-deployer-6567:
        condition: service_completed_successfully

volumes:
  zksyncos_6567_db:
  prometheus_6567_data:
  grafana_6567_data:
  zksync_webhook_db_6567_data:
  bridge_funds_6567_node_modules:
