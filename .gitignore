# Default output directory (wiped and recreated on every run)
out/

# Legacy output dirs
generated/
generated-prividiums/

# Generated/downloaded configs — re-created on each run
# v30.2 chain configs (generated from embedded keys)
configs/v30.2/chain_*.yaml
configs/v30.2/wallets_*.yaml
configs/v30.2/contracts_*.yaml
configs/v30.2/genesis.json
configs/v30.2/keycloak-realm.json
configs/v30.2/prividium-*/

# v31.0 assets — downloaded from upstream on first run (not custom surgery artifacts)
configs/v31.0/

configs/v30.2/.workspace/

# Genesis generator Docker build cache
genesis/.docker-built

# Editor
.DS_Store
*.swp
