# Auto-generated by generate-prividium-compose.sh — do not edit manually.
# Prividium instance for chain ID: 6566
# Ports: zksyncos=5250 admin=3200 user=3201 api=8200 keycloak=5280
name: zksync-prividiums

services:

  # ── keycloak ────────────────────────────────────────────────────────────────
  keycloak-6566:
    image: quay.io/keycloak/keycloak:26.0
    ports:
      - '5280:8080'
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HTTP_RELATIVE_PATH: /
      KC_HEALTH_ENABLED: 'true'
      KC_HTTP_ENABLED: 'true'
      KC_HOSTNAME_STRICT: 'false'
      KC_HOSTNAME_STRICT_HTTPS: 'false'
      KC_HOSTNAME_URL: 'http://localhost:5280'
      KC_HOSTNAME_ADMIN_URL: 'http://localhost:5280'
    command:
      - start-dev
      - --import-realm
    volumes:
      - ./keycloak-realm.json:/opt/keycloak/data/import/realm-export.json:ro
    healthcheck:
      test: ['CMD-SHELL', 'exec 3<>/dev/tcp/127.0.0.1/8080']
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 60s

  # ── zksyncos sequencer ───────────────────────────────────────────────────────
  zksyncos-6566:
    image: ghcr.io/matter-labs/zksync-os-server:latest
    platform: linux/amd64
    ports:
      - '5250:3050'
    environment:
      GENERAL_L1_RPC_URL: 'http://l1:5010'
    user: 'root'
    working_dir: '/app'
    entrypoint: ''
    command: ['/usr/bin/tini', '--', 'zksync-os-server', '--config', '/configs/chain_6566.yaml']
    volumes:
      - zksyncos_6566_db:/db
      - ./chain_6566.yaml:/configs/chain_6566.yaml:ro
      - ./genesis.json:/app/local-chains/v30.2/genesis.json:ro
    depends_on:
      l1:
        condition: service_healthy
    healthcheck:
      test: ['CMD', '/usr/bin/bash', '-c', 'exec 3<>/dev/tcp/127.0.0.1/3050']
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  # ── prividium-api ────────────────────────────────────────────────────────────
  prividium-api-6566:
    image: quay.io/matterlabs_enterprise/prividium-permissions-api:v1.153.1
    platform: linux/amd64
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      zksyncos-6566:
        condition: service_healthy
    ports:
      - '8200:8000'
      - '9291:9091'
    environment:
      - PORT=8000
      - METRICS_PORT=9091
      - SEQUENCER_RPC_URL=http://zksyncos-6566:3050
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/prividium_api_6566
      - CORS_ORIGIN=http://localhost:3200,http://localhost:3201
      - AUTH_METHODS=oidc,crypto_native
      - NODE_ENV=production
      - SIWE_CHAIN_ID=6566
      - SIWE_VALID_DOMAINS=localhost:3200,localhost:3201
      - OIDC_JWKS_URI=http://keycloak-6566:8080/realms/prividium/protocol/openid-connect/certs
      - OIDC_JWT_AUD=prividium-client
      - OIDC_JWT_ISSUER=http://localhost:5280/realms/prividium
      - OIDC_ADMIN_SUBS=00000000-0000-0000-0000-000000000001
      - CORS_CACHE_DURATION_MS=600000
      - ADMIN_PANEL_REDIRECT_URLS=http://localhost:3200/callback
      - BLOCK_EXPLORER_REDIRECT_URLS=http://localhost:3210/auth/callback
      - WALLETS_API_ENABLED=true
      - WEBAUTHN_RP_NAME=ZKsync Prividium
      - WEBAUTHN_RP_ID=localhost
      - WEBAUTHN_ORIGIN=http://localhost:3201
      - WEBAUTHN_REQUIRE_USER_VERIFICATION=false
      - EXTRA_PUBLIC_CODE_ADDRESSES=0x36615cf349d7f6344891b1e7ca7c72883f5dc049

  # ── admin panel ──────────────────────────────────────────────────────────────
  admin-panel-6566:
    image: quay.io/matterlabs_enterprise/prividium-adminv2:v1.153.1
    platform: linux/amd64
    restart: unless-stopped
    environment:
      - VITE_PRIVIDIUM_API_URL=http://localhost:8200
      - VITE_USER_PANEL_URL=http://localhost:3201
      - VITE_EXPLORER_URL=http://localhost:3210
      - PORT=3000
    depends_on:
      - prividium-api-6566
    ports:
      - '3200:3000'

  # ── user panel ───────────────────────────────────────────────────────────────
  user-panel-6566:
    image: quay.io/matterlabs_enterprise/prividium-user-panel:v1.153.1
    platform: linux/amd64
    restart: unless-stopped
    environment:
      - VITE_OIDC_AUTHORITY=http://localhost:5280/realms/prividium
      - VITE_OIDC_CLIENT_ID=prividium-client
      - VITE_OIDC_REDIRECT_URI=http://localhost:3201/callback
      - VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:3201/login
      - VITE_OIDC_BUTTON_TEXT=Sign in with Keycloak
      - VITE_ADMIN_PANEL_REDIRECT_URI=http://localhost:3200/callback
      - VITE_AUTH_METHODS=oidc,crypto_native
      - VITE_CHAIN_ID=6566
      - VITE_CHAIN_NAME=Prividium Local
      - VITE_WALLETS_ENABLED=true
      - VITE_BRIDGING_ENABLED=true
      - VITE_L1_RPC_URL=http://localhost:5010
      - VITE_L1_CHAIN_ID=31337
      - VITE_L1_CHAIN_NAME=Anvil Localhost
      - VITE_PRIVIDIUM_API_URL=http://localhost:8200
      - VITE_EXPLORER_URL=http://localhost:3210
      - VITE_ADMIN_PANEL_URL=http://localhost:3200
      - VITE_REOWN_PROJECT_ID=
      - PORT=3000
    depends_on:
      - prividium-api-6566
    ports:
      - '3201:3000'

  # ── block explorer ───────────────────────────────────────────────────────────
  block-explorer-api-6566:
    image: ghcr.io/matter-labs/block-explorer-api:latest
    platform: linux/amd64
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      LOG_LEVEL: verbose
      NODE_ENV: development
      DATABASE_HOST: postgres
      DATABASE_NAME: prividium_block_explorer_6566
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres
      DATABASE_ENABLE_SSL: 'false'
      NETWORK_NAME: testnet
      PRIVIDIUM: 'true'
      PRIVIDIUM_APP_URL: http://localhost:3210
      PRIVIDIUM_PERMISSIONS_API_URL: http://host.docker.internal:8200
      PRIVIDIUM_SESSION_SECRET: QkxPQ0tfRVhQTE9SRVJfU0VTU0lPTl9TRUNSRVQ=
      PRIVIDIUM_SESSION_MAX_AGE: 86400000
      PRIVIDIUM_SESSION_SAME_SITE: strict
      LIMITED_PAGINATION_MAX_ITEMS: '10000'
      API_LIMITED_PAGINATION_MAX_ITEMS: '1000'
      DISABLE_BFF_API_SCHEMA_DOCS: 'true'
    ports:
      - '3202:3000'
    depends_on:
      postgres:
        condition: service_healthy

  block-explorer-data-fetcher-6566:
    image: ghcr.io/matter-labs/block-explorer-data-fetcher:latest
    platform: linux/amd64
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      NETWORK_NAME: testnet
      BLOCKCHAIN_RPC_URL: http://zksyncos-6566:3050
      PRIVIDIUM: 'true'
      PRIVIDIUM_PERMISSIONS_API_URL: http://host.docker.internal:8200/api
    ports:
      - '3240:3040'

  block-explorer-worker-6566:
    image: ghcr.io/matter-labs/block-explorer-worker:latest
    platform: linux/amd64
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      DATABASE_HOST: postgres
      DATABASE_NAME: prividium_block_explorer_6566
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres
      DATABASE_ENABLE_SSL: 'false'
      NETWORK_NAME: testnet
      BLOCKCHAIN_RPC_URL: http://zksyncos-6566:3050
      DATA_FETCHER_URL: http://block-explorer-data-fetcher-6566:3040
      PRIVIDIUM: 'true'
      PRIVIDIUM_PERMISSIONS_API_URL: http://host.docker.internal:8200/api
      ENABLE_TOKEN_OFFCHAIN_DATA_SAVER: 'true'
      BLOCKS_PROCESSING_BATCH_SIZE: '10'
      NUMBER_OF_BLOCKS_PER_DB_TRANSACTION: '10'
    depends_on:
      postgres:
        condition: service_healthy
      block-explorer-data-fetcher-6566:
        condition: service_started

  block-explorer-app-6566:
    image: ghcr.io/matter-labs/block-explorer-app:latest
    platform: linux/amd64
    volumes:
      - prividium-6566/block-explorer-config.js:/usr/share/nginx/html/config.js:ro
    ports:
      - '3210:3010'
    depends_on:
      block-explorer-api-6566:
        condition: service_started

volumes:
  zksyncos_6566_db:
